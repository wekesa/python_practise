"""
- REGEX IN PYTHON
- Is a sequence of characters that forms a search pattern
- It can be used to check if a string has a specified search term
- ReGEX python module. - python has a built-in regex module/package called re which can be used to work with regular expressions
-
- ReGex functions available:
  - findall: Returns a list containing all matches
  - search: Returns a match object if there is a match anywhere in the string
  - split: returns a list where the string has been split at each match
  - sub: replaces one matches or many matches with a string
"""
import re


